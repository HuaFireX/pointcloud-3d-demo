# 真实点云文件放置目录

把 `.ply` / `.pcd` / `.csv` / `.xyz` / `.txt` 文件放到此目录，demo 中点"加载文件"按钮可选择加载。

CSV 格式约定：
- 至少 3 列，依次为 `x,y,z`（浮点数，单位米）
- 可选第 4 列 `intensity`（0~255 或 0~1）
- 支持带表头或不带表头

示例：
```
x,y,z,intensity
1.234,5.678,0.123,128
1.245,5.679,0.125,130
```
