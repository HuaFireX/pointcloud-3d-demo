# 3D 点云地图管理工具软件设计方案

**版本：V1.1**

**定位：总体软件架构、数据模型、数据一致性与大型点云处理设计**

**修订说明（相对 V1.0）**：本版依据已确认的底层数据组织方式（一个 Map 由多帧 PCD 与一条 alidarState.txt 位姿轨迹组成，PCD 与状态行严格一一对应，位姿为轨迹自身世界坐标系（从 0,0,0 开始构建）下的位姿，描述子由固定帧数窗口生成）对核心数据模型做了实质调整：底层真实数据模型明确为 Map → Trajectory → Frame 三级结构，Submap、Descriptor Window、Costmap 等作为其上的派生组织与任务视图；同时全文改为"每个核心要点整段展开叙述"，数据关系与数据结构用代码块包裹展示，取代原来大量零散的分点罗列。

---

## 1. 项目背景与设计目标

目标系统是一套 3D 点云地图管理工具，用于大型点云地图的加载、显示、旋转、平移、局部编辑、地图拼接、语义标注以及地图成果管理。系统同时服务于两类需求：一类是面向人的地图编辑与查看，包括点云显示、旋转平移、地图拼接、点云删除与恢复、添加二维码、添加虚拟墙、设置不可通行区域、添加特征点与 Landmark 等；另一类是面向机器的地图使用，包括导航与路径规划、Costmap、定位、Submap、回环检测、Descriptor Database、Pose Graph，以及后续可能扩展的语义导航、地图匹配等。

这两类需求之间存在一个核心问题：人在 Surface 上做的编辑，必须能够正确、可追踪地反映到后台真正参与导航、定位和回环检测的数据中。因此系统不能简单地理解成"一个 PCD 文件加 CloudCompare 式编辑"，而应该理解成一个以 Map Core 为中心的地图工程系统：

```text
                  ┌──────── Surface ────────┐
                  │   面向人的可视化/编辑    │
                  └──────────┬─────────────┘
                             │
                         Operation
                             │
                             ▼
                      ┌─────────────┐
                      │  Map Core   │
                      │ 地图核心状态 │
                      └──────┬──────┘
                             │
                       Change Event
                             │
                             ▼
                   Dependency Manager
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
           Costmap       Descriptor     Localization
```

围绕这一核心问题，系统需要满足以下设计目标。
- 建立统一的 Map Core，作为地图对象、几何、位姿、语义、坐标系和版本的共同参照。
- 将 Surface、Navigation、Localization 定义为 Map Core 的不同 Representation 或 Artifact，而不是让任何一个视图成为"唯一真源"。
- 使用稳定的 Map Object ID / UUID 建立点云、帧、Costmap、Descriptor、语义对象之间的关联。
- 将用户操作抽象为 Operation，支持 Undo / Redo、历史记录和后台更新。
- 区分 Geometry Change、Transform Change、Semantic Change、Metadata Change 四类变化，因为它们的传播范围完全不同。
- 支持 Dirty Region 和增量更新，避免局部编辑触发整图重建。
- 通过 Map Version 和 Artifact Source Version 判断后台数据是否过期。
- 保持局部坐标系数据与全局位姿分离，使旋转、平移、拼接尽可能不修改海量点坐标。
- 为 FAST-LIO2 / LIO / SLAM 的 Submap、BTC Descriptor、Costmap 等提供稳定接入接口。
- 为后续 Chunk、Octree、LOD、GPU Streaming 等大型点云优化保留接口。

---

## 2. 底层数据现实与总体架构

### 2.1 已确认的底层数据组织

当前一个 Map 的原始数据就是一个目录，里面是编号的 PCD 文件序列和一条 alidarState.txt 状态文件：

```text
Map001/
├── 1.pcd
├── 2.pcd
├── 3.pcd
├── ...
├── n.pcd
└── alidarState.txt
```

alidarState.txt 的每一行记录一帧状态，字段依次为 timestamp、position（3 维）、quaternion（4 维）、velocity（3 维）、gyro bias（3 维）、accel bias（3 维）、gravity（3 维）、v6（6 维），例如实际数据中的一行：

```text
1735726071.500343                       ← timestamp
-0.0026968 0.0008005 0.0019019          ← p (位置)
0.0124849 0.1964233 0.0119679 0.9803667 ← q (姿态四元数)
1.1400363 0.0826703 -0.0262774          ← v (速度)
...                                     ← bg, ba, g (零偏与重力)
...                                     ← v6 (6 维状态量)
```

结合数据生产链，有几件事实现在已经确定，它们直接决定了数据模型的设计。
- 其一，一个 PCD 严格对应 alidarState.txt 的一行，1.pcd 对应第 1 行、2.pcd 对应第 2 行，一一对应，没有例外。
- 其二，alidarState.txt 中的位姿从 0,0,0 开始，是每次建图时构建的轨迹自身世界坐标系下的 Lidar（或 IMU）位姿；每个 PCD 中的点则是 Lidar（或 IMU——两者固连，只差一个静态外参）原始坐标系下的坐标，位姿与对应点云相乘才能恢复完整地图。
- 其三，描述子由"累计固定帧数的 PCD"生成，具体算法已经存在，本工具暂不深入算法细节，只需在数据模型里预留这一思想。
- 其四，一个 Map 中一个 alidarState.txt 就代表一条完整轨迹。
这四点合起来意味着：当前数据的真实模型是"一张地图 = 一条轨迹 = 一列有序帧"(**其实我更想统一成一个子地图 = 一条轨迹，但是这样会和 voxelslam 中多帧 pcd 累计而成的也叫子地图混淆**)，而不是"一张地图 = 一个点云"，也不是"一张地图 = 若干 Submap"。设计必须从这个事实出发，而不是把 SLAM 里常用的 Submap 概念强行套到已有数据结构上。

### 2.2 Raw Layer 与 Map Core Layer

既然 PCD + alidarState.txt 已经是数据生产链（FAST-LIO2 / LIO）的自然输出，就没有必要在一开始推翻底层格式。建议把它定义为 Raw Layer（原始数据层），原样保留、不做修改，在其上建立 Map Core Layer：

```text
Raw Data (PCD + alidarState.txt)
        │
        ▼
   Map Importer
        │
        ▼
   ┌───────────┐
   │ Map Core  │
   │           │
   │ Frames    │
   │ Trajectory│
   │ Relations │
   │ Semantic  │
   └───────────┘
```

这样做既不打乱现有 LIO 数据生产链，又让上层获得稳定的对象模型。第一阶段甚至可以连 per-frame 的 JSON 都不生成，直接解析 alidarState.txt 与 PCD 目录索引，在内存中构造 MapFrame，只有地图工程本身的元数据需要落盘。

### 2.3 总体数据流

整个系统的推荐逻辑数据流是一条单向链加一条反向接入线。单向链是：
```bash
人 → Surface → Operation → Map Core → Change Event → Dependency Manager → Update Plan → Artifact（Costmap / Descriptor / Localization）。
```
UI 永远不直接调用 updateCostmap()、rebuildDescriptor() 之类的函数，一切修改都以 Operation 的形式进入 Map Core，由 Dependency Manager 根据变化类型决定哪些 Artifact 需要更新。
反向接入线是：FAST-LIO2 / SLAM / Localization 也可以反向写入 Map Core，例如在线 SLAM 产生的新 Submap 或新帧通过 SLAM Adapter 注册为 Map Object，然后由 Map Core 继续驱动 Surface、Costmap、Descriptor 的更新。两条线交汇于 Map Core，它就是系统的共同状态中心；Surface 不是唯一真源，系统中还有 SLAM 产生的 Submap、回环检测产生的 Loop Constraint、Pose Graph 优化后的位姿、Descriptor Database、Costmap、Landmark、Semantic Object 等同样重要的数据。

---

## 3. 核心数据模型：Map → Trajectory → Frame

### 3.1 Frame：最小的原子地图对象

根据已确认的底层数据，Frame（地图帧）应当定义为最底层的原子地图数据对象。这里的 Frame 不是 ROS 里 sensor_msgs 那种转瞬即逝的消息，而是地图工程里的一个持久化对象，由稳定 ID、时间戳、点云引用、帧状态和版本号组成。其中一个关键决策是：Frame ID 必须独立于文件名。目前 1.pcd 与 Frame 1 严格一一对应，但以后文件可能被重命名、合并、分割、转换格式、Chunk 化，而"Frame 1 这个地图对象"本身没有发生变化，所以 Frame ID 应该使用稳定的 UUID，文件名只是 Storage Reference。同理，PCD 本身不应成为 Map Core 的核心对象，它只是几何数据的存储单元；Map Core 记录的是 PointCloudRef，通过它间接引用 PCD 文件。这样即使将来 1.pcd 被转换成 1.chunk、1.laz 或拆成多个 chunk，Map Core 中的 Frame 对象依然稳定：

```text
Frame 001
├── FrameId: UUID（稳定，独立于文件名）
├── timestamp
├── PointCloudRef ──→ 001.pcd（或未来的 chunk / laz / bin）
├── FrameState ──────→ alidarState.txt 第 1 行
└── version
```

FrameState 与 alidarState.txt 的一行严格对应，字段不需要任何解释性转化：

```cpp
struct FrameState
{
    double timestamp;
    Eigen::Vector3d p;     // 位置
    Eigen::Quaterniond q;  // 姿态
    Eigen::Vector3d v;     // 速度
    Eigen::Vector3d bg;    // 陀螺零偏
    Eigen::Vector3d ba;    // 加速度计零偏
    Eigen::Vector3d g;     // 重力
    Eigen::Matrix<double, 6, 1> v6;
};

struct MapFrame
{
    FrameId id;                  // 稳定 UUID，独立于文件名
    Timestamp timestamp;
    PointCloudRef point_cloud;   // 指向 1.pcd 之类的存储引用
    FrameState state;            // 对应 alidarState.txt 的一行
    uint64_t version;            // Geometry / State 版本
};

struct PointCloudRef
{
    StorageId storage_id;
    std::string path;
    BoundingBox local_bbox;
    uint64_t point_count;
    PointCloudFormat format;
};
```

### 3.2 Trajectory：一条完整轨迹

因为已经确认"一个 Map 中一个 alidarState.txt 就代表一条完整轨迹"，Trajectory 就应当成为一个真正的核心对象，而不是可有可无的标注。Trajectory 持有稳定 ID、有序的 Frame ID 列表、局部坐标系定义和版本号；alidarState.txt 本质上就是 Trajectory 的状态存储。当前的一个 Map 目录在 Map Core 中就可以无损地理解成：

```text
Map001
│
└── Trajectory001
      ├── Frame001 ── 1.pcd + State 行 0
      ├── Frame002 ── 2.pcd + State 行 1
      ├── Frame003 ── 3.pcd + State 行 2
      └── ...
```

```cpp
struct Trajectory
{
    TrajectoryId id;
    std::vector<FrameId> frames;
    CoordinateFrame local_frame;   // 轨迹自身坐标系（建图时从 0,0,0 开始构建的世界系）
    uint64_t version;
};
```

将来做地图拼接后，一个 Map 会包含多条 Trajectory，每条 Trajectory 各自维护自己的局部坐标系，这恰好是第 4 节坐标体系设计的基础。

### 3.3 Map 与 Submap 的重新定位

Map 是整个地图工程的统一管理容器：它包含 Trajectory、Semantic Object、Relation、Artifact、Metadata 与 Operation History，但 Map 本身绝不等于一个点云文件。需要特别纠正的是 Submap 的定位：现在没有必要把 Submap 强行放进 Map → Submap → Frame 的层级中，因为当前真实数据不是"Submap A 包含 Frame 1、2，Submap B 包含 Frame 3、4"，而是"一条 Trajectory 包含 Frame 1、2、3、4……"。如果后续 SLAM 算法确实需要 Submap，可以把它定义成一个引用若干 Frame 的派生组织——持有 submap_id、source_frames[]、local_frame、T_map_submap 和 metadata——而不是强迫 Frame 必须属于某个 Submap。这与 Descriptor Window 的处理哲学一致：算法需要的组织单元放在上层，底层只保留最真实的数据关系。

### 3.4 Map Core 设计
"surface" 不是地图管理的唯一真源，当做修改时，实际在操纵 Map Core，Surface 变成了 Map Core 的一个视图/表示，一个子地图由一个抽象层的概念 Map Core 来管理。

```text
Map Core
│
├── Map Object
│     ├── ID
│     ├── Trajectory
│     └── ...
├── Relations
├── Version
├── Semantic
└── Operation
```

---

## 4. 坐标体系：局部状态与地图变换分离
底层数据的坐标系语义需要先说清楚：alidarState.txt 中的位姿从 0,0,0 开始，是每次建图时构建的一个轨迹自身世界坐标系——第一帧的位姿从原点出发，后续每一行的 p/q 都是该世界系下 Lidar（或 IMU）的位姿 T_trajectory_lidar_i；而每个 PCD 中的点则是 Lidar（或 IMU——两者固连，只差一个静态外参）原始坐标系下的坐标 P_lidar_i。只有把位姿与对应点云相乘，才能恢复出完整的地图：P_trajectory = T_trajectory_lidar_i × P_lidar_i。因此位姿并不是"Lidar 原始坐标系下的位姿"，而是轨迹自身世界坐标系下的 Lidar 位姿；单独看 alidarState.txt 或单独看 PCD 都无法还原地图，位姿序列加上逐帧点云才是完整的地图。地图编辑器在此基础上另外维护一个轨迹到地图的变换 T_map_trajectory，完整的变换链为：

```text
P_map = T_map_trajectory × T_trajectory_lidar × P_lidar

其中：
  T_trajectory_lidar   ← 保存在 alidarState.txt：轨迹自身世界系（建图时从 0,0,0 开始构建）下的 Lidar 位姿
  P_lidar              ← PCD 中的点：Lidar（或 IMU）原始坐标系下的坐标
  T_map_trajectory     ← 由地图编辑器维护，编辑操作修改
```

这里必须强调 Trajectory Transform 与 Frame State 是两类不同的数据：alidarState.txt 保存的始终是 T_trajectory_lidar_i，地图编辑器额外保存 T_map_trajectory，二者职责分离，互不覆盖。

这一分离给地图编辑器带来最大的价值。
- 用户把整张地图平移 10 米时，只需要修改 T_map_trajectory，所有 Frame 的 PCD 文件与 alidarState.txt 的状态行完全不变，既不需要遍历上千万个点去改坐标，也不需要重写原始数据文件，描述子更不需要重算。
- 地图拼接同理：把 Map B 拼到 Map A 上时，无论通过人工拖动、ICP、NDT、特征匹配、GNSS 还是回环约束得到 T_A_B，落地的动作都只是修改 T_map_trajectory_B，B 的 PCD、State、Descriptor 全部保持原样。
- 正因为如此，Align 和 Merge 必须严格区分成两个不同的 Operation：Align 只改变换、不产生任何新数据；Merge 才真正产生新的 Geometry、Trajectory、Frame 与 Descriptor。
- 反过来的方向也成立：删除某几帧里的部分点云时，修改的是 Frame 的 Geometry（版本号递增），而轨迹位姿完全不变——几何变化与变换变化是两个相互独立的轴，这也是第 6 节版本与增量更新机制成立的前提。

---

## 5. 派生对象与依赖图：Descriptor 与 Costmap

描述子由"累计固定帧数的 PCD"生成，这是已经确认的算法事实。数据模型需要做的是把它表达为一种派生对象，而不是让 Map Core 理解算法细节。假设窗口大小为 5 帧，Frame 1~5 生成 Descriptor 1，那么 Descriptor 不应该直接"拥有"这些 PCD，也不应该指向某个 PCD 文件，而应该记录它的来源帧 ID 列表，加上数据本体、生成配置和版本号：

```text
Frame 001 ──┐
Frame 002 ──┤
Frame 003 ──┼──→ Descriptor 001
Frame 004 ──┤
Frame 005 ──┘

而不是：

Descriptor 001 ──→ 001.pcd
```

```cpp
struct Descriptor
{
    DescriptorId id;
    std::vector<FrameId> source_frames;   // 来源帧，不是 PCD 路径
    DescriptorData data;
    DescriptorConfig config;
    uint64_t version;
};

struct DescriptorWindow
{
    WindowId id;
    std::vector<FrameId> frames;
    DescriptorId descriptor;
    WindowConfig config;   // 窗口长度 / 步长等，改变它不影响 Map Core
};
```

这样无论窗口是 5 帧、10 帧还是 20 帧，Map Core 只需要知道"这个 Descriptor 由哪些 Frame 产生"这一件事，算法具体怎么算对它没有影响；以后改变窗口长度、步长、累计距离或角度，也不会破坏 Map Core。Submap 在这一语境下也是同理的派生组织，这里不再赘述。

把这一关系展开，整个系统就形成了一张有向无环依赖图（DAG），Dependency Manager 正是这张依赖图在系统运行期的实际落地形式：

```text
                              MAP
                               │
                               │ contains
                               ▼
                          TRAJECTORY
                               │
                               │ contains
                               ▼
                             FRAME
                          ┌────┴────┐
                          │         │
                          ▼         ▼
                    POINT CLOUD   STATE
                          │
                          │ source（来源帧）
                          ▼
                   DESCRIPTOR WINDOW
                          │
                          │ generates
                          ▼
                       DESCRIPTOR
                          │
                          ▼
                  LOCALIZATION DATABASE

        FRAME ────────────────────→ COSTMAP
                                       ▲
        SEMANTIC OBJECT ───────────────┘
        (NoGoArea / VirtualWall / QRCode)
```

设计上需要始终区分三种不同性质的关系，不要混在一起：包含关系（Map 包含 Trajectory，Trajectory 包含 Frame）、组成关系（Frame 由 Geometry 与 State 组成）、依赖关系（Descriptor 依赖若干 Frame；Costmap 依赖 Frame / Semantic Object / Transform）。这是后续设计数据库表结构时最容易出错的地方。

Costmap 的依赖应当绑定到"空间区域 + 源对象版本"，而不是绑定整个 Map Version。例如：

```text
Costmap Tile (10, 20)

source:
    F100 v3
    F101 v7
    F102 v2
    NoGoArea_003 v1
```

当远处某个完全不相干的 F500 被修改时，这个 Tile 不需要任何更新——这对大型地图的增量更新至关重要。

---

## 6. 版本管理与增量更新

版本管理分为三个粒度。
- Map Version 记录地图核心状态的整体版本，每次提交变化递增；
- Object Version 记录单个对象（Frame、Trajectory、语义对象）的版本，每次修改递增；
- Artifact 则记录 source_objects + source_versions，并维护自身状态（CURRENT / STALE / INVALID / BUILDING / FAILED / DELETED）。
之所以不能只用 Map Version，原因很简单：如果只改了一个二维码就让整个 Map Version +1，那么所有 BTC Descriptor 都会被判为过期，这毫无必要。Artifact 记录"我是根据 Frame 100 v3、Frame 101 v2 生成出来的"，过期判断就退化为纯粹的版本比较——当前源对象版本与记录版本不一致即 STALE——完全不需要人工猜测。

在这个机制下，增量更新可以自动推演。假设用户在 Surface 中框选删除一块区域，空间索引找到受影响的是 Frame 100~103：

```text
删除区域横跨 Frame 100 ~ 103
        ↓
F100.GeometryVersion++
F101.GeometryVersion++
F102.GeometryVersion++
F103.GeometryVersion++
        ↓
Dependency Manager 查询：哪些 Descriptor 引用了这些 Frame？
（5 帧窗口 → D001 ~ D003）
        ↓
D001 → STALE，重算
D002 → STALE，重算
D003 → STALE，重算
D004 及以后 → 完全不受影响
```

如果用户删除的是整个 Frame 102，那么引用它的 Window 的 source_frames 变得不完整，对应 Descriptor 标记 INVALID，系统需要重新生成。这里的核心结论是：Frame 才是 Descriptor 依赖关系的最小实体，依赖粒度到帧，不到点、也不到 Map。Costmap 走的是另一条路：按 Dirty Region（OldBBox ∪ NewBBox）做区域更新，受影响的 Costmap、LOD、渲染缓存和空间索引只更新对应区域。这套"版本比较 + 依赖查询 + 区域更新"的组合，就是"Surface → Map Core → Dependency → Artifact"真正落地之后的样子，也是这个系统需要的增量更新机制。

---

## 7. 语义对象与地图编辑

并不是所有地图信息都应该通过修改点云来表达。真实世界中存在点云但规划系统需要的是 NoGoArea；现实世界没有墙但规划系统需要 VirtualWall；二维码本质上是一个带 ID、Pose 和尺寸的 Landmark。因此语义对象应当作为 Map Core 中的一等公民，各自拥有稳定 ID 和版本：

```cpp
NoGoArea  { id; polygon; height; semantic_type; }
VirtualWall { id; start; end; height; semantic; }
QRCode    { id; code; Pose pose; width; height; type; }
Landmark  { id; type; Pose pose; FeatureData feature; }
```

编辑它们只触发 SemanticChanged，不改动几何、不触发描述子重算，影响范围是 Costmap（NoGoArea、VirtualWall 增加障碍约束）或 Localization（若支持二维码定位则更新 Landmark Database）。

有了 Frame / Trajectory 的数据模型之后，各编辑操作的代价可以精确描述。平移、旋转、Align、Pose Graph 优化等操作只修改 Trajectory Transform，Frame Geometry 完全不变，因此成本极低、描述子通常不用重算；NoGoArea、VirtualWall、QRCode 的增删改只动语义；只有"删除局部点云"这一操作真正修改 Frame Geometry。这个性质——大多数地图编辑并不需要修改原始 Frame 几何——是整个系统性能和版本管理得以清晰的关键。另外，删除也不应该是二元的：建议区分 HIDE、DISABLE_FOR_NAVIGATION、DISABLE_FOR_LOCALIZATION、LOGICAL_DELETE、PHYSICAL_DELETE 等层级。例如用户认为某个点云区域"不应该参与导航"时，不必真的删除点云，标记 DISABLE_FOR_NAVIGATION 即可——Surface 仍然显示，PointCloud 保留，Navigation 忽略。这对地图管理非常重要。

---

## 8. Operation 体系：事务、撤销与一致性

Operation 表示一次地图修改，典型类型包括 TransformOperation、DeletePointCloudOperation、MergeOperation、AddQRCodeOperation、AddVirtualWallOperation、SetNoGoAreaOperation、OptimizePoseOperation 等，每个 Operation 携带稳定 ID、类型、目标对象、修改前后的状态以及修改前后的 Map Version：

```cpp
struct Operation
{
    UUID id;
    OperationType type;
    std::vector<UUID> targets;
    State before;
    State after;
    uint64_t map_version_before;
    uint64_t map_version_after;
};
```

UI 永远不直接调用后台更新函数，而是走"用户拖动 → TransformOperation → Map Core 修改 T → TransformChanged → Dependency Manager → Update Plan"这条链，UI 与后台算法由此解耦。Operation Log 同时支撑历史记录、Undo / Redo、审计与网络同步：Undo 即应用反向操作，Redo 即重新应用。复杂操作（如 Merge：修改多个 Submap、生成新几何、重算 Descriptor、更新 Costmap）应当放进 Transaction 中执行——Begin → 若干 Operation → Validate → Commit，失败则 Rollback——绝不允许出现"Surface 已经变了、Costmap 只更新一半、Descriptor 已经删除"的中间状态。

一致性模型采用两层策略：Map Core 强一致，Object State、Object Version、Map Version、Operation Log 在一次 Commit 中原子地保持一致；Artifact 采用最终一致，用户删除点云后 Map Core 与 Surface 立即更新，而 Costmap、Descriptor 进入 BUILDING 状态异步重建，最终到达 CURRENT。这样的分工既保证了编辑响应即时，又保证了后台派生数据不会阻塞交互。

---

## 9. Surface 与大型点云渲染

Surface 层不应该直接管理 1.pcd、2.pcd 直到 10000.pcd 这样的文件列表，它看到的是 Map、Trajectory、Submap、语义对象这些逻辑对象，Frame 位于 Submap 之下、只有在展开时才可见——这对人来说更容易管理。底层"Frame 001 → 001.pcd"与显示层是两套不同的模型：Data Model 与 Rendering Model 可以不同，也必须不同：

```text
Surface 看到的是：                渲染时实际发生的是：

Map                               Submap
├── Submap_001                       ↓
│   ├── Frame 001                 Renderer
│   └── ...                          ↓
├── Submap_002                    加载相关 Frame
├── QRCode                            ↓
├── VirtualWall                    Chunk
└── NoGoArea                          ↓
                                    GPU
```

大型点云不能"加载 PCD → 全部上传 GPU → 每次修改全部重新计算"，而要空间分块：每个 Chunk 保存 BoundingBox、PointCount、StorageRef、LOD 与 GPU Buffer；Octree 负责视锥裁剪、空间查询、Picking、局部编辑、Dirty Region 查询与局部加载；LOD 按距离切换密度，且 LOD 属于 Renderer Artifact，绝不改变 Map Core 的原始几何；用户旋转地图时只改变 Camera / Model Matrix，Point Buffer 保持不变，绝不 CPU 遍历所有点重新计算 XYZ 再重新上传 GPU。这些是大型点云实时操作的前提，但都属于第二阶段之后的性能工作，第一阶段先保证数据模型正确。

---

## 10. 存储与文件组织

存储层面保持 Raw Layer 不动，地图工程在上层逐步增加元数据。推荐的演进形态是：

```text
map_project/
├── raw/
│   ├── 1.pcd
│   ├── 2.pcd
│   ├── ...
│   └── alidarState.txt        ← 原样保留，不修改
│
├── core/
│   ├── map.json               ← Map / Trajectory / Frame 映射
│   ├── trajectory.json
│   └── frame_index.json
│
├── descriptors/
├── navigation/
├── localization/
└── history/
```

第一阶段可以极简——原始目录加上一个 map.json 即可，map.json 只描述 Map、Trajectory、Frame → PCD 与 Frame → State 行号的映射关系，不复制任何原始数据。frame_index.json 是其中最有用的索引，它把 Frame ID 与 PCD 文件、状态行号稳定地关联起来：

```json
{
    "trajectory_id": "trajectory_001",
    "frames": [
        {
            "frame_id": "frame_000001",
            "index": 0,
            "timestamp": 1735726071.500343,
            "point_cloud": "1.pcd",
            "state_line": 0
        },
        {
            "frame_id": "frame_000002",
            "index": 1,
            "timestamp": 1735726071.533676,
            "point_cloud": "2.pcd",
            "state_line": 1
        }
    ]
}
```

这样就建立了 Frame ID → PCD → State → Descriptor → Costmap 之间的稳定联系。具体文件格式（PCD、PLY、LAS / LAZ、自定义 Binary、SQLite）可以后续再决定，当前阶段绝不让文件格式决定 Map Core 的设计。

---

## 11. 实施路线

实施分四个阶段，每个阶段都建立在前一阶段验证过的数据模型之上。
第一阶段（MVP）的目标是验证核心数据模型，而不是追求功能完整：从 PCD + alidarState.txt 无损导入并构造 MapFrame，实现 Surface 的旋转与平移并将操作记录为 TransformOperation，实现两个地图的 Align（暂不做真正的 Merge），添加 NoGoArea、VirtualWall、QRCode 三类语义对象，实现一个简单的 Costmap Artifact，实现 source_version 记录与 STALE 判断，实现 Undo / Redo，实现 Map Project 的保存与重新加载。这一阶段最重要的验收标准是：任意一次 Surface 编辑，都能够明确知道 Map Core 哪个状态发生了变化、哪些 Artifact 需要更新、哪些 Artifact 可以继续复用。

第二阶段接入算法：FAST-LIO2 / LIO 的 Submap 与在线帧接入 Map Core，接入 BTC Descriptor 并建立 Submap → Descriptor 依赖，支持 Loop Constraint、Pose Graph 与回环优化后的位姿更新，实现 Costmap Dirty Region 更新与异步 Artifact Job Queue。

第三阶段解决大型地图性能：Chunk、Octree / 空间索引、LOD、GPU Buffer Cache、视锥裁剪、大地图 Streaming、局部编辑与局部重建、GPU Picking、后台异步加载。

第四阶段工程化：完整地图版本、Snapshot、Transaction、冲突处理、Artifact 可复现构建、算法插件化、多种 Descriptor Backend、地图导入导出、地图发布与地图回滚。

四个阶段的主要风险与对策如下表，其中大多数风险已在数据模型层面给出预防：

| 风险 | 表现 | 对策 |
|---|---|---|
| Surface 与 Backend 耦合 | UI 直接调用大量 update 函数 | Operation + Event + Dependency Manager |
| 反复修改全局点坐标 | 旋转/拼接耗时 | 局部状态 + T_map_trajectory 分离 |
| Artifact 过期无法判断 | 后台数据与地图状态不一致 | Source Version + Artifact Status |
| 整图重建 | 局部修改导致长时间阻塞 | Dirty Region + 帧级依赖增量更新 |
| 误删不可恢复 | 数据丢失 | 分级删除 + Operation Log + Snapshot |
| 算法绑定过深 | 更换 Descriptor 导致 Core 重构 | Descriptor 作为 Artifact，Core 只存来源帧 |
| 文件格式限制架构 | PCD 结构决定业务模型 | Storage 与 Map Core 解耦 |
| SLAM 与人工编辑冲突 | 同时修改地图 | Operation / Transaction / Version |

---

## 12. 当前阶段建议优先确定的内容

在真正开始 Qt / OpenGL / PCL 等具体开发之前，建议优先确定 Map Core 的几个核心接口：

```cpp
MapObjectId createObject(...);
MapObject   getObject(MapObjectId id);
void        applyOperation(const Operation& op);
ChangeSet   getChanges();
UpdatePlan  buildUpdatePlan(const ChangeSet& changes);
ArtifactStatus getArtifactStatus(ArtifactId id);
void        rebuildArtifact(ArtifactId id);
void        undo();
void        redo();
void        commitTransaction();
void        rollbackTransaction();
```

其中 `applyOperation → ChangeSet → buildUpdatePlan` 这条链是最核心的，它决定了后续 Surface、Costmap、Descriptor、Localization 能否真正解耦。同时，Frame ID、Trajectory ID、Map ID、Artifact ID 以及它们之间的引用关系应当最先设计——这几个 ID 一旦设计错误，后面地图编辑、Descriptor 重算、地图版本和回滚都会受到影响。与之相对，Qt 还是其他 GUI、OpenGL 还是 Vulkan、PCL 还是 Open3D、PCD 还是 LAS/LAZ、Octomap 还是自研 Octree、SQLite 还是其他数据库，这些都属于实现层，当前阶段不建议优先确定，等技术栈替换时 Map Core 模型不应受影响。

下一步最值得做的工作，是把本方案具体化成一份 Map Core 数据模型设计：
- C++ 类定义、JSON Schema、文件目录结构、
- Frame / Trajectory / Map 的生命周期、
- 删除 / 移动 / 旋转 / 拼接对应的 Operation、
- Descriptor → Frame 与 Costmap → Frame / Semantic Object 的依赖表、
- Version / Dirty / STALE 机制、
- 如何从现有 PCD + alidarState.txt 无损导入，
- 以及以后如何接 FAST-LIO2 在线生成的新 Frame。

本方案并不是照搬某个现成产品的行业标准，而是针对本项目需求组合成熟思想后形成的架构。它参考了 CloudCompare 的点云对象管理与 Transform 思想，Lanelet2 的稳定唯一 ID 与对象 Relation 思想，Autoware 的"点云地图 / 矢量地图 / 定位地图 / 规划地图按任务分离"的思想，SLAM / Pose Graph 的"局部几何 + 全局位姿"思想，以及软件工程中的事件驱动架构、依赖管理、版本化 Artifact、Undo / Redo、事务与增量更新实践。最终形成的核心数据关系是：Map 包含 Trajectory，Trajectory 包含 Frame，Frame 由 PointCloudRef 与 FrameState 组成；Frame 与 Semantic Object 作为来源指向 Descriptor、Costmap 等派生 Artifact；所有修改通过 Operation 进入 Map Core，通过版本比较与依赖图自动传播到需要更新的后台数据。这套结构首先解决的是数据之间如何建立稳定联系的问题，之后才在 PointCloud Engine 与 Rendering Engine 中解决大型点云的 Chunk、Octree、LOD、GPU Streaming 等性能问题。
